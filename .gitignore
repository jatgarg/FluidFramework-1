.cache-loader
node_modules
dist
lib
!common/lib
nyc
*.log
.DS_Store

# C# port work (transpiledir branch)
# Reference-only copy of the host repo's C# Fluid code; not part of Fluid CI.
packages/dds/waccobalt/
# .NET build output for packages/dds/map/src/csharp-port/
packages/dds/map/src/csharp-port/**/bin/
packages/dds/map/src/csharp-port/**/obj/
# .NET build output for the shared shim project
packages/dds/csharp-port-common/**/bin/
packages/dds/csharp-port-common/**/obj/
# .NET build output for the SharedString port
packages/dds/sequence/src/csharp-port/**/bin/
packages/dds/sequence/src/csharp-port/**/obj/

.vale/*
!.vale/config/

.vale/config/*
!.vale/config/vocabularies/

.vale/config/vocabularies/*
!.vale/config/vocabularies/fluid/

# Generated Node10 module resolution compatibility files
alpha.d.ts
beta.d.ts
internal.d.ts
legacy.d.ts

# TypeScript incremental build cache
*.tsbuildinfo

# c8 code coverage temporary files
/coverage/tmp/

# Temporary api-extractor build artifacts
**/_api-extractor-temp/**

# api-extractor metadata artifact
# This should not be generically ignored nor checked in. If found, examine settings
# to see if file is meant to be generated in that location.
# tsdoc-metadata.json

# Bundle analysis artifacts
bundleAnalysis
bundleAnalyzerJson
compareBundlesOutput

# Misc pipeline artifacts
artifacts

website/resources/_gen/

# PNPM store (when mounting host file system in docker container)
.pnpm-store/

# Scratch dir created by `flub check trustPolicy`.
**/.trust-audit-temp/

# TODO: This can be removed once the `flub add changeset` command no longer creates the UPCOMING file.
UPCOMING.md

# Ignore the build output archive folder
# This is created to later publish its content on the pipeline and do not need to be included on git as
# artifacts or bundleAnalysis folders. See scripts/pack-build-output.sh for more details.
build_output_archive

# Claude Code
# Local settings contain author-specific information and should not be shared.
**/.claude/**/*.local.*
CLAUDE.local.md
# Session-specific task tracking
**/.claude/todos/
# Git worktrees created by Claude Code
**/.claude/worktrees/

# Output from benchmarks
**/benchmark*Output.json

# npm configuration: Currently only used by CI to redirect to an alternative registry.
.npmrc
